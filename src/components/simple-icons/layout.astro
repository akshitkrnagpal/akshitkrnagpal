---
export type { Props } from 'simple-icons-astro'

const size = Astro.props.size
delete Astro.props.size
---

<svg
	xmlns="http://www.w3.org/2000/svg"
	width={size ?? 24}
	height={size ?? 24}
	fill="currentColor"
	viewBox="0 0 24 24"
	{...Astro.props}
>
	<slot />
</svg>